const sum = (a, b) => a + b

const mult = (a, b) => a * b 
const name = 'Artem'

export  {   //экспорт нескольких переменных
    mult,
    name
}
export default sum // экспортировать переменную из этого модуля 

